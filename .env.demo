# Demo Environment Configuration for QPerform
# Use this for demo deployments where you want to show the app without backend

# QPerform Backend API URL (for demo, can point to mock or real backend)
VITE_API_URL=http://localhost:3001

# QSoftware Backend URL (used for authentication)
VITE_QSOFTWARE_API_URL=https://api.qsoftware.cloud/api

# Mock Authentication (set to 'true' for demo without needing Microsoft auth)
VITE_USE_MOCK_AUTH=true
